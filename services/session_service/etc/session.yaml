# 服务基础配置
main_config:
  app_name: "session_service"
  host: "127.0.0.1"
  grpc_port: 9002       # 会话服务gRPC端口
  http_port: 8002       # 会话服务HTTP端口（如需）

# 数据库配置
mysql_config:
  host: "127.0.0.1"
  port: 3306
  user: "root"
  password: "123456"
  database_name: "gogochat"

# Redis配置
redis_config:
  host: "127.0.0.1"
  port: 6379
  password: "123456"
  db: 0

# etcd 配置
etcd_config:
  host: "127.0.0.1"
  port: 2379

# 日志配置
log_config:
  log_path: "./services/session_service/logs"
