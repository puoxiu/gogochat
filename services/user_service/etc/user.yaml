# 服务基础配置
main_config:
  app_name: "user_service"
  host: "127.0.0.1"
  grpc_port: 9001       # 用户服务gRPC端口
  http_port: 8001       # 用户服务HTTP端口（如需）

# 数据库配置
mysql_config:
  host: "127.0.0.1"
  port: 3306
  user: "root"
  password: "123456"
  database_name: "gogochat"

# Redis配置
redis_config:
  host: "127.0.0.1"
  port: 6379
  password: "123456"
  db: 0

# etcd 配置
etcd_config:
  host: "127.0.0.1"
  port: 2379



# 短信服务配置
auth_code_config:
  access_key_id: "your_aliyun_access_key"
  access_key_secret: "your_aliyun_secret"
  sign_name: "GoGoChat"
  template_code: "SMS_154950909"


# 日志配置
log_config:
  log_path: "./services/user_service/logs"